import 'package:flutter/material.dart';

var lightColor=ColorScheme.fromSeed(seedColor: Colors.teal,
  brightness: Brightness.light,
);
var darkColor=ColorScheme.fromSeed(seedColor: Colors.teal,
  brightness: Brightness.dark,
);